ETUDE_IMPORTANT:
   SCENARIO : Uma
   ANT_COORD_GEN : g
   UE_COORD_GEN: a
   COORD_FILES:  # Utilisé quand les coordonées sont lues à partir d'un fichier.
      # read: data/coordonnees_case8.txt # actif quand on lis le fichier de coordonnées 
      #Ajout de commentaire à la ligne précédante
      write: data/sortie_case8.txt   # actif quand on veut écrire les résultats dans un fichier
   DEVICES:
      Antenna1 : # Group Antenna1 (comme défini dans la base de données)
         number : 25
      UE1-App1 : # Group UE1-App1 (comme défini dans la base de données)
         number : 20
      UE2-App2 : # Group UE2-App2 (comme défini dans la base de données)
         number: 30
   VISIBILITY : # read nlos information
      read: visibility1.txt 
   GEOMETRY:
      Surface:
         rectangle:
           length : 1000 # en mètres
           height : 500